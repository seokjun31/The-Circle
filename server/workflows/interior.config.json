{
  "_comment": "Injection point config for interior.json workflow. Change node IDs here when the workflow changes.",
  "injections": {
    "input_image": {
      "node": "13",
      "field": "image",
      "description": "LoadImage node — receives the room photo with mask as alpha channel"
    },
    "positive_prompt": {
      "node": "6",
      "field": "text",
      "description": "CLIPTextEncode positive prompt node"
    },
    "negative_prompt": {
      "node": "7",
      "field": "text",
      "description": "CLIPTextEncode negative prompt node"
    },
    "seed": {
      "node": "3",
      "field": "seed",
      "description": "KSampler seed — auto-randomized on each run"
    }
  }
}
